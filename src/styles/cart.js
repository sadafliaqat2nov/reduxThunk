import {StyleSheet} from 'react-native';
import * as utils from '../utilities/index';

const stylesCart = StyleSheet.create({
  customIcon: {
    justifyContent: 'center',
    backgroundColor: utils.COLOR_DARK_ORANGE,
    borderRadius: 15,
    alignItems: 'center',
    height: 17,
    width: 17,
    marginBottom: 7,
  },
});

export default stylesCart;
